FROM python:3.11-slim
WORKDIR /app
RUN pip install fastapi uvicorn[standard] jinja2
COPY app /app
ENV BASIC_USER=admin BASIC_PASS=admin
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
