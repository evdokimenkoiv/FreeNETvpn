# Quickstart
Enable optional services with `sudo python3 tools/manage.py services all`, then `sudo bash install.sh --existing`. Create profiles from Connections, assign them from People and open their device guides. Download an off-server backup before upgrade. See `docs/recovery.en.md` and `docs/recovery.ru.md` for restore and its validation boundaries.
